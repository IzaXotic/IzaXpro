{
  "name": "izaxpro",
  "version": "1.0.0",
  "description": "IzaXpro Business Management Suite by IzaXotic",
  "main": "app.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "start": "node app.js",
    "dev": "concurrently \"npm run server:dev\" \"npm run client\"",
    "server": "cd server && npm start",
    "server:dev": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd server && npm install && cd ../client && npm install && npm run build",
    "render-build": "npm install && cd server && npm install && cd ../client && npm install && npm run build",
    "render-start": "cd server && node index.js",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["business", "document", "generation", "invoice", "quotation", "project-management"],
  "author": "Your Business Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}